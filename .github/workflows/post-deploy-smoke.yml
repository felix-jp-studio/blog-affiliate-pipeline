name: Post-deploy smoke

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "scripts/e2e/changed-slugs.mjs"
      - "scripts/e2e/smoke-production.mjs"
      - "config/e2e-smoke.json"
      - ".github/workflows/post-deploy-smoke.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Resolve smoke slugs from changed articles
        id: slugs
        run: |
          SLUGS="$(node scripts/e2e/changed-slugs.mjs --format=comma)"
          echo "slugs=$SLUGS" >> "$GITHUB_OUTPUT"
          echo "Smoke slugs: $SLUGS"

      - name: Production smoke test
        env:
          E2E_SMOKE_SLUGS: ${{ steps.slugs.outputs.slugs }}
        run: npm run test:e2e:smoke

      - name: Verify IndexNow key file (production)
        if: secrets.INDEXNOW_KEY != ''
        env:
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
        run: node scripts/e2e/verify-indexnow-key.mjs --target=production

      - name: Write job summary
        if: always()
        run: |
          {
            echo "## Post-deploy smoke"
            echo ""
            echo "- target: https://sim-hikari-guide.com"
            echo "- script: \`npm run test:e2e:smoke\`"
            echo "- indexnow: \`npm run test:e2e:indexnow\` when \`INDEXNOW_KEY\` secret is set"
            echo "- config: \`config/e2e-smoke.json\`"
            echo "- retry: exponential backoff (max 5 attempts, CDN delay)"
          } >> "$GITHUB_STEP_SUMMARY"
